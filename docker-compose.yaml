version: '3.8'

services:
  bot:
    build: .
    container_name: water_calorie_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    stdin_open: true
    tty: true                   
